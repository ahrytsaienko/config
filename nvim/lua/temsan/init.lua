require("temsan.options")  -- options & leader (must run before lazy)
require("temsan.autocmds") -- non-plugin autocommands
require("temsan.lazy")     -- plugin manager + all specs
require("temsan.remap")    -- non-plugin keymaps
